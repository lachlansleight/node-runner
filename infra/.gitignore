# Secrets — never commit
terraform.tfvars
*.auto.tfvars

# Terraform state & working dir
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
